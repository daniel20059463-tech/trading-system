# 給 Fable 的稽核清單（審核 Opus 的工作）

**目的**：你(Fable)是第二雙眼睛。Opus 會犯錯（本清單末列出它已知犯過的錯）。
你的任務**不是重訓**（那是固定程式，換誰跑都一樣），而是：**挑出 bug、挑戰驗證的漏洞、質疑結論**。
金融 ML 最致命的是「資料洩漏(look-ahead)」——一個對齊錯，準確率就是假的。請把每個「日期對齊」當嫌疑犯查。

---

## A. 找 bug：精度關鍵檔案（一個數字錯就誤導買賣）

### 1. `quantile_forecast.py`（區間預測）
- `predict_quantile()` 裡的 **±10% 漲跌停夾限**：夾完覆蓋率有沒有被破壞？（Opus 說「不影響覆蓋率」，請驗證這句）
- **新聞感知不對稱**：`news_bull` 撐高 `q90/q75` 的公式，會不會讓 q75 > q90（交叉）？
- **p_up 混合**：`0.4*p_up + 0.6*73` 這個強利多混合——加權合理嗎？會不會過度樂觀？
- 視窗切法 `[-WINDOW_SIZE:]` 有沒有 off-by-one？

### 2. `models/predict.py` → `predict_verification()`（命中率的來源！）
- 我(Opus)在這加了**強利多方向修正**（`adjust_direction`）。**重點查**：
  - 它用 `df.index[-1].date()` 當新聞日——這是「被預測的那天」，對嗎？會不會用到**未來**的新聞（look-ahead）？
  - 盤前預測(predict_ensemble)沒加這修正、盤後(predict_verification)有——這個**不一致**會不會讓命中率虛高？

### 3. `strong_news_override.py` / `news_aux_judge.py`
- `today_score()` 讀「今日盤前新聞」——盤前預測時新聞**還沒抓**（predict 在 news 之前跑），這個時序有沒有問題？
- 門檻 0.20 是用**全樣本**選的（in-sample），會不會過擬合？

### 4. `models/train.py` / `run_retrain.py`（訓練）
- scaler 是否**只 fit 訓練集**？（Opus 聲稱有防洩漏，請驗證 `prepare_dataset`）
- 「不退步閘門」：`48.1%→61.5% 才採用`——這個比較是在**同一個 held-out**上嗎？還是拿訓練集自評？

---

## B. 挑戰驗證方法（我可能設計錯）

1. **強利多 OOS 73%**：`_us_tw_relationship` 之外，強利多的 out-of-sample 只有 **15 個測試樣本**。15 筆的 73% 信賴區間多寬？這個 edge 可能是雜訊嗎？
2. **關鍵字權重**：live 用的權重是**全樣本**算的（keyword_impact.json），只有「驗證那次」用訓練期。**live 系統本身有沒有洩漏**？
3. **「籌碼是同時不是領先」**：我用「D日外資→D+1漲跌」測得 +6%。但如果外資是**收盤後才公布**，那 D日的數字要到 D日晚上才知道，用它預測 D+1 是合理的——**我的對齊對嗎？還是我其實少算了一天**？
4. **國際新聞 GDELT**：我用「隔夜對齊」測 tone→台股。但 GDELT 是**全球 24 小時**的日資料，我的「隔夜」定義對嗎？

---

## C. Opus 已知的弱點 / 犯過的錯（請針對這些多查）

- ✅ **±10% 漲跌停 bug**：Opus 一開始沒考慮台股單日上限，區間給到 +16%（不可能）。→ 還有沒有**其他物理上限**沒考慮？(如個股暫停交易、除權息跳空)
- ✅ **國際新聞「沒資料」**：Opus 假設沒有就放棄，其實 GDELT 有。→ 還有哪些「我說沒有」其實**有**的資料源？
- ✅ **GDELT mode 打錯**：查了半天以為限速，其實參數錯。→ 其他 API 呼叫有沒有類似的靜默錯誤？
- ⚠️ **小樣本**：很多結論建立在 15~80 個樣本上。→ 哪些結論**樣本不足以下定論**？
- ⚠️ **in-sample 門檻**：0.20、0.25 這些門檻多在全樣本選的。→ 哪些是過擬合？

---

## D. 值得試的「未測方向」（但要守紀律：≥65% 或明顯贏基準才建，沒過就退）

1. **只預測「大動作日」**：不猜漲跌，改猜「明天會不會有 >±5% 大動作」（分類問題，可能比方向好預測）
2. **regime 換模型**：平靜日用一個模型、異常日用另一個（我們有 drift monitor 可切）
3. **方向感知 loss 全面用**：目前只有 <50% 的股票用，全部改用會如何？
4. **per-stock vs pooled**：目前每股各自訓練，混合訓練會不會更穩？

**⚠️ 但先講**：Opus 已測 7 個特徵全失敗，56% 很可能是效率市場天花板。上述若你測完也沒過門檻，**請如實說沒過**，別為了交差硬凹——這個專案的紀律是「測過才建、沒過就退」。

---

## 交付格式（請這樣回報）

- **找到的 bug**：檔案:行號 + 問題 + 建議修法
- **驗證漏洞**：哪個結論的證據不足/有洩漏
- **未測方向**：若你測了，附**樣本數 + 上漲率 + 是否過門檻**，過才建議建
- **最重要**：如果你**推翻了「56% 天花板」**——用嚴格驗證(防洩漏、OOS、夠樣本)證明——Opus 第一個服。但如果你也撞牆，請誠實說「Opus 是對的」。
