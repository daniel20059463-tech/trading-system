# Fable 稽核報告（2026-07-02）

依 `FABLE_稽核清單.md` 逐項審核。方法：通讀核心程式 → 逐嫌疑點查證 → 可量化的跑實證。
結論先講：**沒有找到「假準確率」級的資料洩漏，但找到一個嚴重的選擇性偏誤、
一條證據不足的關鍵結論，以及數個一致性缺陷。天花板結論成立，且可能比 Opus 說的更低。**

---

## 一、找到的問題（按嚴重度排序）

### 🔴 F1【方法缺陷】「不退步閘門」的 48.1%→61.5% 是選擇性偏誤，數字虛高

- **位置**：`models/train.py:134-138`（val_loader ＝測試集）＋ `train.py:201-204`
  （`select_metric="direction"` 挑「測試集方向準確率最高」的 epoch）＋
  `eval_harness.py:52`（`_predict_testset` 又用**同一測試集**評分）
- **問題**：200 個 epoch 裡挑測試集表現最好的權重，再用同一測試集報成績
  ——這是教科書級的 selection bias。61.5% 不是泛化能力，是「挑出來的最好一次」。
  凡經過方向重訓的股票，「平均 56.1%」都含此向上偏誤。
- **失敗情境**：某股真實能力 50%，隨機波動下 200 次抽樣的最大值輕鬆上 60%+，
  閘門判定「進步」採用新模型，實際上什麼都沒變好。
- **修法**：把測試段對半切成「選擇用 cal」與「報告用 holdout」（quantile_forecast.py
  的 CQR 已經這樣做了，同一招搬過來即可），或改 walk-forward。
- **重要含義**：這**強化**而非推翻天花板結論——真實天花板可能低於 56%。

### 🔴 F2【證據不足】強利多 73% 的證據鏈有三個洞

a) **統計不顯著**：OOS 只有 15 樣本 11 勝。實算：p=0.059（vs 銅板50%）、
   **p=0.120（vs 測試期基準55%）**。門檻標準（p<0.05）下不成立。
b) **回測對齊 ≠ live 對齊**：回測是「FinMind 日 T 新聞 → T+1 方向」；
   live 是「當日 08:30 LLM 摘要 → 當日方向」。**兩個不同的預測目標**，
   73% 不直接支持 live 用法。且 keyword_impact.json 兩種對齊混存
   （實查：FinMind 慣例 4,230 筆＋盤前慣例 190 筆），權重語意混濁。
c) **文字分布錯配＋動能回聲**：回測權重學自新聞標題；live 評分文字是
   **LLM 產物**（key_catalysts/strategy_reason）。實查 7/2 強茂的評分文字含
   「股價勁揚逾7%」「訂單出貨比續飆高」——**標題在報導昨天的上漲**。
   所以「強利多分數高」很大成分是「昨天大漲的回聲」，
   這個訊號更像**動能延續**，不是新鮮資訊。動能不是不能用，但要誠實命名，
   且模型的價格特徵理論上已含動能（雖然模型常在這些日子看跌——值得深究為何）。
- **另**：門檻 0.20 為全樣本選點（Opus 已自認），in-sample。
- **修法**：①用 live 同款對齊（早上新聞→當日方向）重做 OOS；
  ②每次觸發記 log，累積 30+ 筆真實 live 樣本再驗收；
  ③兩種對齊分開存表。**在此之前，73% 應標示為「初步、未驗證」**。

### 🟡 F3【一致性】盤後命中率衡量的是「從未在盤前發布過的策略」

- **位置**：`models/predict.py` `predict_verification()` 內的 `adjust_direction`
- **查證結果**：**沒有未來資訊洩漏**——08:30 盤前新聞早於 09:00 開盤，時序合法；
  且 `record_today` 在驗證**之後**跑（orchestrator 順序已確認），驗證日 D 時
  關鍵字權重只含到 D-1。這兩點 Opus 做對了。
- **但**：盤前推給用戶的點預測（`predict_all`/`predict_ensemble`）**沒有**套用修正，
  盤後驗證**有**。成績單追蹤的是一個盤前根本沒發布過的混合策略——
  命中率會比「用戶實際看到的預測」好看。
- **修法**：盤前同步套用同一修正；或成績單分列「原始模型 / 修正後」兩欄。

### 🟡 F4【小瑕疵群】quantile_forecast.py

1. `:301` 註解寫「0.25+/74%」，`:303` 程式碼是 0.20/73——註解過期。
2. **p_up 對新聞雙重計入**：news_bull 先撐高 q90/q75（抬高內插 p_up），
   再混 `0.4*p_up+0.6*73`。同一訊號影響兩次；且模型自身 p_up>73 時
   混合反而**往下拉**（如 90%→79.8%）——「強利多讓機率變低」的怪行為。
3. `_p_up` 邊界：q10==0 時走 `0<=pts[0][0]` 分支回 F0=0.05，理論上應為 0.10，
   p_up 報 95% 略偏高。
4. 兩個門檻不一致：news_bull>0.05 就撐上界、≥0.20 才動 p_up——
   弱利多 0.06 也會撐寬區間上緣，設計依據不明。
5. 夾限順序與不交叉保護**驗證無誤**；「實際漲跌不可能超±10%」
   實掃 10 檔近 2 年 CSV：**0 天超過 10.5%**，本宇宙成立。
   但除權息／減資日理論上可破（此期間未發生），建議加註監控。

### ⚪ F5【邊界重疊】`prepare_dataset` 滑窗跨界

- 訓練樣本的標籤最多深入測試區前緣約 `0.2×WINDOW=6` 行（滑窗通病）。
  影響極小，提示即可；嚴格做法是 train/test 之間留 WINDOW 天緩衝。

---

## 二、查證後「無罪」的項目（清單質疑但實際沒問題）

| 質疑 | 查證結果 |
|------|---------|
| scaler 洩漏 | ✅ 無。`prepare_dataset` 只 fit `data[:split]`（train.py:70-72）|
| 籌碼「少算一天」 | ✅ 無。三大法人於 D 日盤後公布，用 D 日資料測 D+1 合法，+6% 結論站得住 |
| 驗證日權重洩漏 | ✅ 無。`record_today` 在盤後驗證之後執行，權重只含 D-1 前 |
| GDELT 隔夜對齊 | ✅ 大致合理（UTC 日界略糊，但 tone 本來就無訊號，不影響結論）|
| 夾限破壞覆蓋率 | ✅ 未破壞（實掃 0 天 >10.5%）|

---

## 三、未測方向（我沒有跑新實驗，如實說）

時間所限，D 區四個方向我**沒有實測**，不假裝有結果。若要做，最值得的是
**D1 大動作日分類**（猜「明天是否 |漲跌|>3%」而非方向）——波動聚集是
金融時序少數真實存在的可預測結構（GARCH 效應），比方向可測。
設計：特徵用近 5/20 日已實現波動＋量比，label=|明日 pct|>3%，
walk-forward 驗證，AUC>0.65 才算數。守同一紀律：沒過就退。

---

## 四、天花板判定（清單要求正面回答）

**我沒有推翻「~56% 天花板」——反而發現它可能虛高。**

- F1 的選擇性偏誤讓「56.1%」含向上成分；修正後真實數字可能更接近 52-54%。
- 18 天 live 追蹤的 64% 是誠實的 walk-forward，但含多個 100% 極端順勢日，樣本太小。
- 唯一的 edge（強利多 73%）證據不足（p=0.12），且機制疑似動能回聲。

**Opus 的方向是對的：隔日方向逼近效率市場極限。它的錯不在結論，
在把幾個樂觀數字（61.5%、56.1%、73%）當成比實際更硬的證據。**

---

## 五、建議修正優先序 → 已全部執行（2026-07-02）

1. ✅ **F1 已修**：`train.py` 方向選擇只用測試段前半(cal)挑 checkpoint；
   `eval_harness` 閘門改評後半(holdout)。實測朋程 holdout=55.8%（機制生效）。
2. ✅ **F2 已修**：`strong_news_override.py` 降級標示「初步」；每次觸發自動記
   `strong_news_log.json`，`python strong_news_override.py` 出 live 驗收報告（30+筆再下結論）。
3. ✅ **F3 已修**：orchestrator 盤前預測在新聞分析後同步套用 adjust_direction，
   盤前發布＝盤後驗證，成績單不再好看於用戶所見。
4. ✅ **F4 已修**：p_up 改用未含新聞的分位數計算（單次計入）；混合改
   `max(raw, 0.4raw+0.6·73)` 只升不降（實測強茂 46.5%→62.4%✅）；
   撐寬門檻統一 0.15 起、0.30 滿；`_p_up` 邊界改嚴格不等；註解同步。
5. ✅ **F5 已註記**：`prepare_dataset` docstring 說明滑窗邊界重疊與嚴格解法。
